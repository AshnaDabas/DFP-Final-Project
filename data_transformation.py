def setup_company_data(company_list, input_source_std1, input_source_std2, input_source_std3):
    print("Setting up company data")
    company_dict = dict(zip(iter(company_list), ''))
    return company_dict